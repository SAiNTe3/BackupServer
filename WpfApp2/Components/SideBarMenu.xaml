<UserControl x:Class="BackupServer.Components.SideBarMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008">
    <UserControl.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#9AA0AC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="30,10"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E4E8EC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#E4E8EC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MenuTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="宋体"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#F0F3F6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Border Grid.Row="0" Background="Transparent" Padding="20,40">
                <TextBlock Text="菜单" Foreground="#5A6274" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
            </Border>
            
            <StackPanel Grid.Row="1">
                <Button Style="{StaticResource MenuButtonStyle}" Click="ServerPanelButton_Click">
                    <TextBlock Text="服务器面板" FontSize="16"/>
                </Button>
                <Button Style="{StaticResource MenuButtonStyle}">
                    <TextBlock Text="设置" FontSize="16"/>
                </Button>
                <Button Style="{StaticResource MenuButtonStyle}">
                    <TextBlock Text="本地浏览" FontSize="16"/>
                </Button>
                <Button Style="{StaticResource MenuButtonStyle}">
                    <TextBlock Text="漫游" FontSize="16"/>
                </Button>
                <Button Style="{StaticResource MenuButtonStyle}">
                    <TextBlock Text="关注" FontSize="16"/>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>